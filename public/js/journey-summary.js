document.addEventListener('DOMContentLoaded', () => {
    // Placeholder: in future we can add sorting, filters, or row expansion
    // For now, this keeps the file wired correctly if you want to extend.
    console.debug('Journey summary JS loaded');
});
